export default defineNuxtConfig({
  modules: [
    '@nuxtjs/tailwindcss',
    // Modul lain jika perlu
  ],
  build: {
    // Pengaturan build jika perlu
  },
  css: [
    // Mengimpor file CSS khusus
  ]
})
